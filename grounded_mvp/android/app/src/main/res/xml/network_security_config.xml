<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow plain HTTP to local network addresses so Ollama on your PC is reachable.
        This does NOT affect HTTPS traffic to the internet (Gemini etc).
    -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Local WiFi ranges -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">10.0.2.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <!-- Android emulator host -->
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

    <!-- Everything else uses default (HTTPS only for external traffic) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
